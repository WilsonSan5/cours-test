{% extends 'base.html.twig' %}

{% block title %}Éditer liste #{{ todo_list.id }} — TodoList{% endblock %}

{% block body %}
<div class="page-header">
    <h1>✏️ Éditer liste <span style="opacity:.6">#{{ todo_list.id }}</span></h1>
    <a href="{{ path('app_todo_list_show', {'id': todo_list.id}) }}" class="btn btn-secondary">← Retour</a>
</div>

<div class="card" style="max-width:480px;">
    {{ include('todo_list/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
</div>

<hr class="divider" style="max-width:480px;">

{{ include('todo_list/_delete_form.html.twig') }}
{% endblock %}
