{% extends 'base.html.twig' %}

{% block title %}TodoList{% endblock %}

{% block body %}
    <h1>TodoList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ todo_list.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ todo_list.createdAt ? todo_list.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_todo_list_index') }}">back to list</a>

    <a href="{{ path('app_todo_list_edit', {'id': todo_list.id}) }}">edit</a>

    {{ include('todo_list/_delete_form.html.twig') }}
{% endblock %}
